# ─── OpenClaw Admin 配置 ───
# 复制本文件为 .env 并按需修改。所有路径均为绝对路径。

# OpenClaw 主目录路径（默认 ~/.openclaw）
# macOS:   /Users/yourname/.openclaw
# Linux:   /home/yourname/.openclaw
# Windows: C:\Users\yourname\.openclaw
# OPENCLAW_HOME=/Users/yourname/.openclaw

# 系统日志目录（OpenClaw 网关运行时重定向的日志位置）
# 不设置时按平台回退：
#   macOS:   ~/Library/Logs/openclaw
#   Linux:   ${XDG_STATE_HOME:-~/.local/state}/openclaw/logs
#   Windows: %LOCALAPPDATA%\openclaw\logs
# 如你的 OpenClaw 使用自定义日志重定向路径，请在此指定：
# OPENCLAW_SYS_LOGS_DIR=/var/log/openclaw

# ─── 以下为 OpenClaw 自身的配置（非本工具读取，仅供参考）───
# OpenClaw 的 ~/.openclaw/.env 中可配置各插件的 API Key，
# openclaw.json 中可用 $VAR / ${VAR} 引用这些变量，
# 本工具会自动解析并显示真实值（点击眼睛图标可切换显示）。
